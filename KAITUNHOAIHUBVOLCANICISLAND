--//========================================================
--// HOÀI HUB
--// Kaitun volcanic island A-Z
--// Purple UI + Safe Fix Lag + Auto Marines
--// Status luôn: STATUS: Waiting...
--//========================================================

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

--========================================================
-- CONFIG
--========================================================

local CONFIG = {
	Title = "HOÀI HUB",
	Subtitle = "Kaitun volcanic island A-Z",

	Logo = "rbxassetid://82514517939235",

	Accent = Color3.fromRGB(165, 70, 255),
	AccentLight = Color3.fromRGB(215, 150, 255),

	Background = Color3.fromRGB(13, 10, 18),

	MenuSize = UDim2.fromOffset(310, 120),

	MenuPosition = UDim2.new(
		0.5, -155,
		0.5, -60
	),

	StatusPosition = UDim2.new(
		0.5, -155,
		0.5, -105
	),

	FloatingPosition = UDim2.new(
		0, 24,
		0.5, -27
	)
}

--========================================================
-- REMOVE OLD GUI
--========================================================

local OldGui = PlayerGui:FindFirstChild("HoaiHub")

if OldGui then
	OldGui:Destroy()
end

--========================================================
-- SCREEN GUI
--========================================================

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "HoaiHub"
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = PlayerGui

--========================================================
-- STATUS
--========================================================

local Status = Instance.new("TextLabel")

Status.Name = "Status"

Status.Size = UDim2.fromOffset(310, 24)
Status.Position = CONFIG.StatusPosition

Status.BackgroundTransparency = 1

Status.Font = Enum.Font.GothamMedium
Status.Text = "STATUS: Waiting..."
Status.TextColor3 = CONFIG.AccentLight
Status.TextSize = 12
Status.TextXAlignment = Enum.TextXAlignment.Center

Status.ZIndex = 50
Status.Parent = ScreenGui

local function SetStatus(Text)
	Status.Text = "STATUS: " .. tostring(Text)
end

_G.HoaiHubSetStatus = SetStatus

--========================================================
-- MAIN MENU
--========================================================

local Main = Instance.new("Frame")

Main.Name = "Main"

Main.Size = CONFIG.MenuSize
Main.Position = CONFIG.MenuPosition

Main.BackgroundColor3 = CONFIG.Background
Main.BackgroundTransparency = 0.03
Main.BorderSizePixel = 0

Main.ZIndex = 10
Main.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 9)
MainCorner.Parent = Main

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = CONFIG.Accent
MainStroke.Thickness = 2.5
MainStroke.Parent = Main

--========================================================
-- LOGO
--========================================================

local MenuLogo = Instance.new("ImageLabel")

MenuLogo.Name = "MenuLogo"
MenuLogo.BackgroundTransparency = 1

MenuLogo.Size = UDim2.fromOffset(42, 42)
MenuLogo.Position = UDim2.new(0, 10, 0, 10)

MenuLogo.Image = CONFIG.Logo
MenuLogo.ScaleType = Enum.ScaleType.Fit

MenuLogo.ZIndex = 11
MenuLogo.Parent = Main

--========================================================
-- TITLE
--========================================================

local Title = Instance.new("TextLabel")

Title.Name = "Title"
Title.BackgroundTransparency = 1

Title.Position = UDim2.new(0, 50, 0, 10)
Title.Size = UDim2.new(1, -60, 0, 30)

Title.Font = Enum.Font.GothamBold
Title.Text = CONFIG.Title
Title.TextColor3 = Color3.fromRGB(245, 238, 255)
Title.TextSize = 19
Title.TextXAlignment = Enum.TextXAlignment.Center

Title.ZIndex = 11
Title.Parent = Main

--========================================================
-- SUBTITLE
--========================================================

local Subtitle = Instance.new("TextLabel")

Subtitle.Name = "Subtitle"
Subtitle.BackgroundTransparency = 1

Subtitle.Position = UDim2.new(0, 10, 0, 56)
Subtitle.Size = UDim2.new(1, -20, 0, 28)

Subtitle.Font = Enum.Font.GothamSemibold
Subtitle.Text = CONFIG.Subtitle
Subtitle.TextColor3 = Color3.fromRGB(205, 175, 235)
Subtitle.TextSize = 13
Subtitle.TextXAlignment = Enum.TextXAlignment.Center

Subtitle.ZIndex = 11
Subtitle.Parent = Main

--========================================================
-- FLOATING LOGO
--========================================================

local FloatButton = Instance.new("ImageButton")

FloatButton.Name = "FloatingLogo"

FloatButton.Size = UDim2.fromOffset(55, 55)
FloatButton.Position = CONFIG.FloatingPosition

FloatButton.BackgroundColor3 = CONFIG.Background
FloatButton.BackgroundTransparency = 0.02
FloatButton.BorderSizePixel = 0

FloatButton.Image = CONFIG.Logo
FloatButton.ScaleType = Enum.ScaleType.Fit

FloatButton.AutoButtonColor = false

FloatButton.ZIndex = 100
FloatButton.Parent = ScreenGui

local FloatCorner = Instance.new("UICorner")
FloatCorner.CornerRadius = UDim.new(1, 0)
FloatCorner.Parent = FloatButton

local FloatStroke = Instance.new("UIStroke")
FloatStroke.Color = CONFIG.Accent
FloatStroke.Thickness = 2.5
FloatStroke.Parent = FloatButton

--========================================================
-- MENU SHOW / HIDE
--========================================================

local MenuVisible = true

local function ShowMenu()
	MenuVisible = true
	Main.Visible = true

	Main.Size = UDim2.fromOffset(0, 0)

	local Tween = TweenService:Create(
		Main,
		TweenInfo.new(
			0.22,
			Enum.EasingStyle.Back,
			Enum.EasingDirection.Out
		),
		{
			Size = CONFIG.MenuSize
		}
	)

	Tween:Play()
end

local function HideMenu()
	MenuVisible = false

	local Tween = TweenService:Create(
		Main,
		TweenInfo.new(
			0.16,
			Enum.EasingStyle.Quad,
			Enum.EasingDirection.In
		),
		{
			Size = UDim2.fromOffset(0, 0)
		}
	)

	Tween:Play()

	Tween.Completed:Connect(function()
		if not MenuVisible then
			Main.Visible = false
		end
	end)
end

FloatButton.MouseButton1Click:Connect(function()
	if MenuVisible then
		HideMenu()
	else
		ShowMenu()
	end
end)

--========================================================
-- DRAG FLOATING LOGO
--========================================================

local Dragging = false
local DragStart
local StartPosition

FloatButton.InputBegan:Connect(function(Input)

	if Input.UserInputType == Enum.UserInputType.MouseButton1
		or Input.UserInputType == Enum.UserInputType.Touch then

		Dragging = true
		DragStart = Input.Position
		StartPosition = FloatButton.Position

		Input.Changed:Connect(function()
			if Input.UserInputState == Enum.UserInputState.End then
				Dragging = false
			end
		end)

	end

end)

UserInputService.InputChanged:Connect(function(Input)

	if not Dragging then
		return
	end

	if Input.UserInputType == Enum.UserInputType.MouseMovement
		or Input.UserInputType == Enum.UserInputType.Touch then

		local Delta = Input.Position - DragStart

		FloatButton.Position = UDim2.new(
			StartPosition.X.Scale,
			StartPosition.X.Offset + Delta.X,

			StartPosition.Y.Scale,
			StartPosition.Y.Offset + Delta.Y
		)

	end

end)

--========================================================
-- SAFE EFFECT DISABLE
--========================================================

local function DisableEffect(Object)

	if Object:IsA("ParticleEmitter")
		or Object:IsA("Trail")
		or Object:IsA("Beam")
		or Object:IsA("Fire")
		or Object:IsA("Smoke")
		or Object:IsA("Sparkles")
		or Object:IsA("Highlight") then

		pcall(function()
			Object.Enabled = false
		end)

		return
	end

	if Object:IsA("BloomEffect")
		or Object:IsA("BlurEffect")
		or Object:IsA("ColorCorrectionEffect")
		or Object:IsA("DepthOfFieldEffect")
		or Object:IsA("SunRaysEffect") then

		pcall(function()
			Object.Enabled = false
		end)

	end
end

--========================================================
-- OPTIMIZE PART
--========================================================

local function OptimizePart(Object)

	if not Object:IsA("BasePart") then
		return
	end

	pcall(function()
		Object.Material = Enum.Material.SmoothPlastic
		Object.CastShadow = false
		Object.Reflectance = 0
	end)

end

--========================================================
-- HIDE MAP TEXTURES
--========================================================

local function HideMapTexture(Object)

	if Object:IsA("Texture")
		or Object:IsA("Decal") then

		pcall(function()
			Object.Transparency = 1
		end)

	end

end

--========================================================
-- HIDE NPC CLOTHES
--========================================================

local function HideNPCClothes(Character)

	if not Character:IsA("Model") then
		return
	end

	local Humanoid = Character:FindFirstChildOfClass("Humanoid")

	if not Humanoid then
		return
	end

	-- Giữ nguyên trang phục người chơi
	if Character == LocalPlayer.Character then
		return
	end

	for _, Object in ipairs(Character:GetDescendants()) do

		-- Accessory / tóc / mũ
		if Object:IsA("Accessory") then

			for _, Part in ipairs(Object:GetDescendants()) do

				if Part:IsA("BasePart") then

					pcall(function()
						Part.LocalTransparencyModifier = 1
						Part.CastShadow = false
					end)

				elseif Part:IsA("Texture")
					or Part:IsA("Decal") then

					pcall(function()
						Part.Transparency = 1
					end)

				end

			end

		-- Shirt
		elseif Object:IsA("Shirt") then

			pcall(function()
				Object.ShirtTemplate = ""
			end)

		-- Pants
		elseif Object:IsA("Pants") then

			pcall(function()
				Object.PantsTemplate = ""
			end)

		-- ShirtGraphic
		elseif Object:IsA("ShirtGraphic") then

			pcall(function()
				Object.Graphic = ""
			end)

		end
	end

end

--========================================================
-- PROCESS OBJECT
--========================================================

local function OptimizeObject(Object)

	-- Không đụng UI HOÀI HUB
	if Object:IsDescendantOf(PlayerGui) then
		return
	end

	DisableEffect(Object)
	OptimizePart(Object)
	HideMapTexture(Object)

end

--========================================================
-- LIGHTING
--========================================================

pcall(function()

	Lighting.GlobalShadows = false

	Lighting.EnvironmentDiffuseScale = 0
	Lighting.EnvironmentSpecularScale = 0

	Lighting.FogEnd = 100000

end)

for _, Object in ipairs(Lighting:GetChildren()) do
	DisableEffect(Object)
end

Lighting.ChildAdded:Connect(function(Object)

	task.defer(function()
		if Object.Parent then
			DisableEffect(Object)
		end
	end)

end)

--========================================================
-- PROCESS EXISTING WORKSPACE
--========================================================

for _, Object in ipairs(Workspace:GetDescendants()) do
	OptimizeObject(Object)
end

--========================================================
-- EXISTING NPC
--========================================================

for _, Object in ipairs(Workspace:GetDescendants()) do

	if Object:IsA("Model")
		and Object:FindFirstChildOfClass("Humanoid") then

		HideNPCClothes(Object)

	end

end

--========================================================
-- NEW WORKSPACE OBJECTS
--========================================================

Workspace.DescendantAdded:Connect(function(Object)

	task.defer(function()

		if not Object.Parent then
			return
		end

		OptimizeObject(Object)

		local Character =
			Object:FindFirstAncestorOfClass("Model")

		if Character
			and Character:FindFirstChildOfClass("Humanoid")
			and Character ~= LocalPlayer.Character then

			HideNPCClothes(Character)
		end

	end)

end)

--========================================================
-- PLAYER CHARACTER
--========================================================

LocalPlayer.CharacterAdded:Connect(function(Character)

	task.wait(0.2)

	-- Chỉ tối ưu đồ họa, không xóa character
	for _, Object in ipairs(Character:GetDescendants()) do

		if Object:IsA("BasePart") then
			OptimizePart(Object)
		else
			DisableEffect(Object)
		end

	end

end)

--========================================================
-- LOW RENDER QUALITY
--========================================================

pcall(function()
	settings().Rendering.QualityLevel =
		Enum.QualityLevel.Level01
end)

--========================================================
-- AUTO SELECT MARINES
--========================================================
-- Chạy ngầm, KHÔNG thay đổi STATUS
-- STATUS vẫn giữ "Waiting..."

task.spawn(function()

	if not game:IsLoaded() then
		game.Loaded:Wait()
	end

	local Remotes =
		ReplicatedStorage:WaitForChild(
			"Remotes",
			30
		)

	if not Remotes then
		warn("HOÀI HUB: Remotes not found")
		return
	end

	local CommF_ =
		Remotes:WaitForChild(
			"CommF_",
			30
		)

	if not CommF_ then
		warn("HOÀI HUB: CommF_ not found")
		return
	end

	-- Đã có team thì không làm gì
	if LocalPlayer.Team then
		return
	end

	-- Tự chọn Marines
	local Deadline = os.clock() + 15

	while LocalPlayer.Parent
		and LocalPlayer.Team == nil
		and os.clock() < Deadline do

		pcall(function()

			CommF_:InvokeServer(
				"SetTeam",
				"Marines"
			)

		end)

		task.wait(0.5)

	end

	if LocalPlayer.Team then
		print(
			"HOÀI HUB: Team = "
				.. LocalPlayer.Team.Name
		)
	else
		warn(
			"HOÀI HUB: Marine selection failed"
		)
	end

end)

--========================================================
-- FORCE STATUS BACK TO WAITING
--========================================================

Status.Visible = true
SetStatus("Waiting...")

--========================================================
-- PURPLE MENU GLOW
--========================================================

task.spawn(function()

	while ScreenGui.Parent do

		local A = TweenService:Create(
			MainStroke,

			TweenInfo.new(
				1.2,
				Enum.EasingStyle.Sine,
				Enum.EasingDirection.InOut
			),

			{
				Color = CONFIG.AccentLight,
				Thickness = 3
			}
		)

		A:Play()
		A.Completed:Wait()

		local B = TweenService:Create(
			MainStroke,

			TweenInfo.new(
				1.2,
				Enum.EasingStyle.Sine,
				Enum.EasingDirection.InOut
			),

			{
				Color = CONFIG.Accent,
				Thickness = 2.5
			}
		)

		B:Play()
		B.Completed:Wait()

	end

end)

--========================================================
-- FLOATING LOGO GLOW
--========================================================

task.spawn(function()

	while ScreenGui.Parent do

		local A = TweenService:Create(
			FloatStroke,

			TweenInfo.new(
				1,
				Enum.EasingStyle.Sine,
				Enum.EasingDirection.InOut
			),

			{
				Color = CONFIG.AccentLight,
				Thickness = 3.5
			}
		)

		A:Play()
		A.Completed:Wait()

		local B = TweenService:Create(
			FloatStroke,

			TweenInfo.new(
				1,
				Enum.EasingStyle.Sine,
				Enum.EasingDirection.InOut
			),

			{
				Color = CONFIG.Accent,
				Thickness = 2.5
			}
		)

		B:Play()
		B.Completed:Wait()

	end

end)

--========================================================
-- READY
--========================================================

Main.Visible = true
Status.Visible = true

print("HOÀI HUB: UI Loaded")
print("HOÀI HUB: Fix Lag Active")
print("HOÀI HUB: Auto Marines Active")
--========================================================
-- HOÀI HUB - KAITUN STYLE PROGRESS CHECKER
-- Dojo Belt -> Dragon Storm -> Dragonheart
-- Check Dojo mỗi 0.1s
--========================================================

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Player = Players.LocalPlayer

--========================================================
-- STATUS
--========================================================

local function SetStatus(Text)
	if _G.HoaiHubSetStatus then
		_G.HoaiHubSetStatus(Text)
	end
end

--========================================================
-- BELT DATA
--========================================================

local Belts = {
	White = {
		English = "White Belt",
		Vietnamese = "đai Trắng"
	},

	Yellow = {
		English = "Yellow Belt",
		Vietnamese = "đai Vàng"
	},

	Orange = {
		English = "Orange Belt",
		Vietnamese = "đai Cam"
	},

	Green = {
		English = "Green Belt",
		Vietnamese = "đai Xanh Lá"
	},

	Blue = {
		English = "Blue Belt",
		Vietnamese = "đai Xanh Dương"
	},

	Purple = {
		English = "Purple Belt",
		Vietnamese = "đai Tím"
	},

	Red = {
		English = "Red Belt",
		Vietnamese = "đai Đỏ"
	},

	Black = {
		English = "Black Belt",
		Vietnamese = "đai Đen"
	}
}

local BeltOrder = {
	"White",
	"Yellow",
	"Orange",
	"Green",
	"Blue",
	"Purple",
	"Red",
	"Black"
}

--========================================================
-- GET DOJO PROGRESS
--========================================================

local Remotes = ReplicatedStorage:WaitForChild("Remotes")

local DojoRemote =
	Remotes:WaitForChild("RF/InteractDragonQuest")

local function GetDojoProgress()

	local Success, Result = pcall(function()

		return DojoRemote:InvokeServer({
			NPC = "Dojo Trainer",
			Command = "RequestQuest"
		})

	end)

	if not Success then
		return nil
	end

	if type(Result) ~= "table" then
		return nil
	end

	return Result
end

--========================================================
-- GET NEXT BELT
--========================================================

local function GetNextBelt(CurrentBelt)

	if typeof(CurrentBelt) ~= "string" then
		return "White"
	end

	for Index, BeltName in ipairs(BeltOrder) do

		if BeltName == CurrentBelt then

			local NextIndex = Index + 1

			if BeltOrder[NextIndex] then
				return BeltOrder[NextIndex]
			end

			return nil
		end

	end

	return "White"
end

--========================================================
-- DRAGON ITEM CHECK
--========================================================
-- Phần này chỉ kiểm tra những item client đang expose.
-- Không dùng nó để xác định Dojo Belt.

local function HasClientItem(...)

	local Names = {...}

	local function Scan(Container)

		if not Container then
			return false
		end

		for _, Object in ipairs(Container:GetDescendants()) do

			for _, Name in ipairs(Names) do

				if string.lower(Object.Name) == string.lower(Name) then
					return true
				end

			end

		end

		return false
	end

	if Scan(Player.Character) then
		return true
	end

	if Scan(Player:FindFirstChildOfClass("Backpack")) then
		return true
	end

	return false
end

local function HasDragonStorm()

	return HasClientItem(
		"Dragon Storm",
		"Dragonstorm"
	)
end

local function HasDragonHeart()

	return HasClientItem(
		"Dragonheart",
		"Dragon Heart",
		"DragonHeart"
	)
end

--========================================================
-- STATUS CHECK
--========================================================

local LastStatus = ""

local function UpdateStatus()

	local Progress = GetDojoProgress()

	if not Progress then
		return
	end

	local CurrentBelt = Progress.BeltName

	--============================================
	-- CHƯA CÓ BELT / CHƯA BẮT ĐẦU
	--============================================

	if not CurrentBelt then

		local Text =
			"Getting White Belt / Đang lấy đai Trắng..."

		if Text ~= LastStatus then
			LastStatus = Text
			SetStatus(Text)
		end

		return
	end

	--============================================
	-- CÒN BELT
	--============================================

	if CurrentBelt ~= "Black" then

		local NextBelt = GetNextBelt(CurrentBelt)

		if NextBelt and Belts[NextBelt] then

			local Info = Belts[NextBelt]

			local Text =
				"Getting "
				.. Info.English
				.. " / Đang lấy "
				.. Info.Vietnamese
				.. "..."

			if Text ~= LastStatus then
				LastStatus = Text
				SetStatus(Text)
			end

		end

		return
	end

	--============================================
	-- BLACK BELT -> DRAGON STORM
	--============================================

	if not HasDragonStorm() then

		local Text =
			"Getting Dragon Storm / Đang lấy Dragon Storm..."

		if Text ~= LastStatus then
			LastStatus = Text
			SetStatus(Text)
		end

		return
	end

	--============================================
	-- DRAGON STORM -> DRAGONHEART
	--============================================

	if not HasDragonHeart() then

		local Text =
			"Getting Dragonheart / Đang lấy Dragonheart..."

		if Text ~= LastStatus then
			LastStatus = Text
			SetStatus(Text)
		end

		return
	end

	--============================================
	-- HOÀN THÀNH
	--============================================

	local Text =
		"Complete / Đã hoàn thành!"

if Text ~= LastStatus then
		LastStatus = Text
		SetStatus(Text)
	end
end

--========================================================
-- CHECK LOOP
--========================================================

task.spawn(function()

	while Player.Parent do

		pcall(UpdateStatus)

		task.wait(0.1)

	end

end)

-- Cho Kaitun dùng sau này
_G.HoaiHubGetDojoProgress = GetDojoProgress

print("HOÀI HUB: Kaitun-style Progress Checker Active")
